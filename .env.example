// Database (shared Postgres — auth schema host)
DATABASE_URL=

// Auth (BETTER_AUTH_SECRET MUST match learn-languages for cross-subdomain SSO)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=

// SMTP (mail transport — read directly from env, no longer from DB)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
